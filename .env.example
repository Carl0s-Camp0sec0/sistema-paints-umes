# ==============================================
# CONFIGURACIÓN SISTEMA PAINTS
# ==============================================

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sistema_paints

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=localhost

# JWT Configuration
JWT_SECRET=paints_system_jwt_secret_key_2025_very_secure_umes
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5500,http://127.0.0.1:5500,http://localhost:5501,http://127.0.0.1:5501

# Backup Configuration
BACKUP_PATH=./database/backup/automatic
BACKUP_FREQUENCY=daily

# Email Configuration (Simulación académica)
EMAIL_SERVICE=gmail
EMAIL_USER=sistema.paints@umes.edu.gt
EMAIL_PASS=password_simulado_academico

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/sistema-paints.log

# Security
BCRYPT_ROUNDS=12
SESSION_SECRET=sistema_paints_session_secret_2025

# File Upload (si se necesita)
MAX_FILE_SIZE=5MB
UPLOAD_PATH=./uploads
